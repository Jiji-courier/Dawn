function SearchBar() {
    return (
        <div>
            <input type="text" placeholder = "Search for a book name" />
            <button>Search</button>
        </div>
    )
}

export default SearchBar;